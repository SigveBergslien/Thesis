MAX_DIFF 2048		// How rocky terrain is
PASSES 9		// Needs to be 9 for 256 * 256 map
RND 64			// Random value for initial 4 map-corner values
BORDER 5		// Border at which base and paths cannot be written
BASE_RADIUS 20		// Radius of start and end base
ROUTE_RADIUS 5		// Radius of route points and path
ROUTEPOINTS 10		// Number of random routepoints placed between start and end
SECTORS 16		// Number of sectors along each axis to split map into